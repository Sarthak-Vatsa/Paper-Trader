package com.papertrader.app.data.remote

/**
 * Network layer — implemented in Phase 3 (Networking Layer).
 *
 * Planned classes:
 *  - StockApiService    (Retrofit interface — Yahoo Finance query for .NS/.BO tickers)
 *  - StockQuoteResponse (Gson DTO mapping the API JSON response)
 *  - NetworkModule      (Hilt module providing OkHttpClient + Retrofit singleton)
 */
